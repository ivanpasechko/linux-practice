# linux-practice
born 29.06

# Отчет о выполнении практической работы "Базовые принципы администрирования Linux"

**Выполнил:** Пасечко Иван

## 1. Схема сети и адресация
В ходе работы развернуты 3 виртуальные машины на базе Ubuntu Server 20.04:
* **Linux A (pasechko_server):** Сервер (`192.168.29.10/24`)
* **Linux B (pasechko_gateway):** Маршрутизатор (`192.168.29.1/24`, `192.168.6.1/24`)
* **Linux C (pasechko_client):** Клиент (`192.168.6.100/24`)

## 2. Ход выполнения работы

### Шаг 1. Настройка сетевых интерфейсов и маршрутизации
Конфигурационные файлы Netplan применены на всех машинах. На машине Linux B активирован IP Forwarding.
*(Вставь сюда скриншот команды `ip a` или `ip route` с машин)*

### Шаг 2. Развертывание HTTP-сервера на Linux A
Написан скрипт на Python, обрабатывающий эндпоинты `/get`, `/post`, `/put`. Сервер добавлен в автозагрузку через Systemd.
*(Вставь скриншот вывода команды `sudo systemctl status webserver.service`)*

### Шаг 3. Настройка сетевой безопасности на Linux B
Настроены правила `iptables`, разрешающие прохождение только TCP трафика на порт 5000. Включен перехват пакетов утилитой `tcpdump`.
*(Вставь скриншот запущенного tcpdump)*

### Шаг 4. Проверка связи с Linux C
С помощью утилиты `curl` выполнены три запроса к серверу А. Запросы прошли успешно, ответы получены.
*(Вставь скриншот успешного выполнения curl-запросов и скриншот логов tcpdump с машины B, фиксирующих этот трафик)*

<img width="1650" height="1001" alt="curls logs" src="https://github.com/user-attachments/assets/5ecc9c84-9eff-492a-a137-63e2951eb1a6" />


## Заключение
Архитектура сохраняет свою работоспособность после перезагрузки благодаря конфигурациям Netplan, `iptables-persistent` и `systemd`.

